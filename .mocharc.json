{
  "require": [
    "ts-node/register/transpile-only",
    "src/test/setup.ts"
  ],
  "extensions": ["ts"],
  "spec": ["src/test/unit/**/*.test.ts"],
  "timeout": 10000,
  "color": true,
  "recursive": true,
  "reporter": "spec",
  "ui": "tdd",
  "ts-node": {
    "project": "tsconfig.test.json"
  }
}
